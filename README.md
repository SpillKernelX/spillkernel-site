# spillkernel-site

Landing page for `spillkernel.com`.

## Local preview

```powershell
python -m http.server 4173
```

Then open `http://localhost:4173`.

## Deploy

```powershell
vercel deploy -y
```
